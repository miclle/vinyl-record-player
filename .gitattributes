# Preserve CAD exchange files verbatim and prevent line-based merges.
cad/*.step binary
cad/*.FCStd binary

# Generated PDF streams must not be whitespace-normalized or line-merged.
output/pdf/*.pdf binary
